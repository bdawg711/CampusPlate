Progress_Sprint9.txt
CampusPlate — Sprint 9 Progress Tracker
Last updated: March 2026

Instructions for Claude Code: After completing each task, mark it [x].
Do not mark a task complete unless it has been tested and confirmed working.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 1 — src/utils/nutrition.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] Add extra_active to ActivityLevel type (multiplier 1.9)
[x] Replace percent-based goal multipliers with fixed kcal offsets
    moderate_cut → -300, aggressive_cut → -500, maintain → 0,
    lean_bulk → +300, aggressive_bulk → +500
[x] Add aggressive_bulk to GoalType type
[x] Add calculateTargetCalories() with guardrails
    (female min 1200, male min 1500, max 4500)
[x] Keep calculateTDEE() unchanged (Mifflin-St Jeor already correct)
[x] Remove getWeeklyProjection() or keep as-is (do not break callers)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 2 — src/utils/goals.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] Remove 0.8g/lb flat protein rule
[x] Implement protein g/kg by goal
    moderate_cut 2.1, aggressive_cut 2.3, maintain 1.7,
    lean_bulk 1.8, aggressive_bulk 1.75
[x] Implement fat % by goal
    moderate_cut 25%, aggressive_cut 25%, maintain 30%,
    lean_bulk 27%, aggressive_bulk 25%
[x] Implement carb backfill (calories - protein - fat)
[x] Add edge case: if carbs_g < 0 reduce fat 5% and recalculate once
[x] Remove high_protein boolean from calculation logic
[x] Update recalculateGoals() signature to accept all profile fields
[x] Add getProteinRange(goal, weight_kg) → { min, max }
[x] Water goal: clamp(weight_lbs, 64, 160) instead of flat 64oz

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 3 — app/onboarding.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] Remove current motivational follow-up step
[x] Add intensity step (shown only for lose/build goals)
    lose: Steady (-300) / Aggressive (-500)
    build: Lean (+300) / Aggressive (+500)
[x] Add extra_active option to activity level selector
[x] Update getGoalType() to map goal + intensity → GoalType
[x] Confirm aggressive_bulk saves correctly to profiles.goal
[x] Water goal on onboarding uses new bodyweight-based default

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 4 — src/components/EditGoals.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] Add "Recalculate Goals" button
[x] Fetch full profile, run recalculateGoals()
[x] Show preview of new numbers before saving
[x] On Apply: save to Supabase and update local state
[x] On Cancel: dismiss with no changes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 5 — Weight Recalibration Prompt
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NOTE: Implemented in app/(tabs)/progress.tsx (logWeight function)
instead of EditProfile.tsx — this is the primary weight logging flow.

[x] After saving new weight, compare to profiles.weight
[x] If difference >= 5 lbs: show recalibration Alert
[x] On "Recalculate": run recalculateGoals(), save to Supabase
[x] On "Not now": dismiss silently
[x] If difference < 5 lbs: save silently, no alert

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KNOWN RISKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- recalculateGoals() is called from multiple places — confirm all
  callers are updated after goals.ts changes
- Onboarding step count change may affect progress bar if it's
  hardcoded to a number of steps
- aggressive_bulk is a new DB value — confirm profiles.goal
  column accepts it (TEXT type, no enum constraint)
